import React from 'react';

const LoadingSpinner: React.FC = () => {
  // This div will be styled by the .loader class we added to index.css
  return <div className="loader"></div>;
};

export default LoadingSpinner;